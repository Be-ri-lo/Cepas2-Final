<%= form_with(model: enologist_wine) do |form| %>
  <% if enologist_wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enologist_wine.errors.count, "error") %> prohibited this enologist_wine from being saved:</h2>

      <ul>
        <% enologist_wine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="field">
    <%= form.label :wine_id %>
    <%= form.text_field :wine_id %>
  </div>

  <div class="field">
    <%= form.label :enologist_id %>
    <%= form.text_field :enologist_id %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
